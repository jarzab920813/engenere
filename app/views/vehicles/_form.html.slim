= simple_form_for [@vehicle] do |f|
  / = f.error_notification
  .form-inputs
    = f.input :name, label: t("vehicle.labels.name"), :wrapper => :horizontal_form
    = f.button :submit, "Zapisz"

  .hr-line-dashed
  .form-group
    .col-sm-4.col-sm-offset-2
  = link_to 'cofnij', vehicles_path 